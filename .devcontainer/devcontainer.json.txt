{
  "name": "C# (.NET MAUI)",
  "image": "mcr.microsoft.com/devcontainers/dotnet:7.0", // Use the .NET SDK
  "postCreateCommand": "dotnet restore", // Automatically restore packages
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "extensions": [
    "ms-dotnettools.csharp",
    "ms-azuretools.vscode-docker"
  ],
  "remoteUser": "vscode",
  "features": {
    "docker-in-docker": "latest"
  }
}
